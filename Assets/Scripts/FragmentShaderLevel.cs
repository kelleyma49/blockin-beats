using UnityEngine;
using System.Collections;

public class FragmentShaderLevel : AbstractLevel
{
    public FragmentShaderLevel() : base(null)
    {
    }

    // Use this for initialization
    void Start()
    {
    }

    // Update is called once per frame
    public override void Update()
    {
        base.Update();
    }
}
